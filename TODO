# [x] after env -i export PATH=/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:/usr/local/munki:/Library/Apple/usr/bin
# [x] + in export 


mkdir -p 1/2/3
rm -rf 1


## [x] unset a b c d e f!!!!!!!

##cat | << cat

# [ ] return 1 f ms_pars
# [x] old pwd in -i ./minishell
# [ ] sort export



# TODO test changit is_hidden & oldpwd

# >$abc

# echo < | |
# bash: syntax error near unexpected token `|'
# bash-3.2$ echo $?
# 258
``


# [ ] < udfb cat | ls > f2 > f3 > f4 double free

# ls > $USERR

# ls >> ''
# syntax error near unexpected token `'

# ls >> ''o
# syntax error near unexpected token `o'

# MINISHELL$ ls >> 's'
# syntax error near unexpected token `s'

# MINISHELL$ ls >> "s"
# syntax error near unexpected token `s'

# [ ]modify the params passed to ft_execute 
# and pass them to handle dir !!

# [ ]echo after sig ctr + c